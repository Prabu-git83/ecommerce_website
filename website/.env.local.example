NEXT_PUBLIC_API_URL=http://localhost:4000/v1

# --- Production (Vercel) ---
# NEXT_PUBLIC_API_URL=/v1                       # browser calls go through the same-origin rewrite
# API_URL=https://<api-project>.vercel.app/v1   # server-side calls
# API_ORIGIN=https://<api-project>.vercel.app   # rewrite target for /v1/*
